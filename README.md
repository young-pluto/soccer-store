# ⚽ Soccer Store (React + Node + SQLite)

Minimalist, production-ready e‑commerce demo for premium soccer gear. Clean aesthetics (Zara/H&M‑inspired), real product images, per‑session carts, and a simple checkout flow.

- Frontend (Vercel): add your deployment URL
- Backend (Render): add your deployment URL
- Repo: add your GitHub repo URL

## ✨ Features
- Real soccer catalog (boots, jerseys, balls, gear) stored in SQLite
- Per‑session shopping cart (no auth needed). Each device/session sees its own cart
- Add, update, remove, and clear cart; live totals
- Minimalist luxury UI, responsive on mobile/tablet/desktop
- Product image presentation optimized (object‑fit: contain, larger imagery)
- Checkout with mock receipt
- Centralized error handling on backend
- Ready for local dev and cloud deploy (Render + Vercel)

## 🧱 Tech Stack
- Frontend: React (CRA), Axios, CSS
- Backend: Node.js, Express 5
- Database: SQLite (file: `backend/commerce.db`)
- Deploy: Vercel (frontend), Render (backend)

## 🗺️ Architecture
Frontend calls Backend REST API under `/api/*`. Each request includes an `x-session-id` header (autogenerated and persisted in `localStorage`) which the backend uses as `userId` to scope cart rows. This keeps carts isolated per device/session without a full auth system.

## 🚀 Quick Start (Local)
1) Backend
```bash
cd backend
npm install
npm start   # starts at http://localhost:5050
```

2) Frontend
```bash
cd frontend
npm install
echo "REACT_APP_API_URL=http://localhost:5050" > .env
npm start   # opens http://localhost:3000
```

Notes
- If port 5000 is taken on macOS (AirPlay), this project uses 5050 by default.
- If you see 403s in dev, bypass proxy by using the `.env` above.

## 🌐 Deploy
This repo is structured to deploy with:
- Backend → Render (Node Web Service)
  - Root: `backend`
  - Build Command: `npm install`
  - Start Command: `npm start`
  - Env: `NODE_ENV=production` (Render sets `PORT`)
- Frontend → Vercel (Create React App)
  - Root: `frontend`
  - Env Var: `REACT_APP_API_URL=https://YOUR_RENDER_SERVICE.onrender.com`

See `DEPLOYMENT.md` for step‑by‑step screenshots and commands.

## 🔌 API Reference
All requests should include an `x-session-id` header (frontend adds it automatically).

Products
- `GET /api/products` → `{ products: Product[] }`
- `GET /api/products/:id` → `{ product }`

Cart (scoped per session/user)
- `GET /api/cart` → `{ cart: CartItem[], total: number }`
- `POST /api/cart` `{ productId, quantity }` → add or increment
- `PUT /api/cart/:id` `{ quantity }` → update quantity
- `DELETE /api/cart/:id` → remove item

Checkout
- `POST /api/checkout` `{ name, email }` → mock receipt and clears session cart

## 📁 Project Structure
```
vibe-commerce/
├── backend/
│   ├── routes/            # products, cart, checkout
│   ├── database.js        # SQLite schema + seed
│   ├── server.js          # Express app, session, error handler, static build
│   └── commerce.db        # auto-created SQLite database file
├── frontend/
│   ├── src/components/    # React components
│   ├── src/App.js         # Router + pages
│   ├── src/App.css        # Minimalist luxury theme (light)
│   └── src/index.js       # Sets x-session-id header globally
└── README.md
```

## 🧪 What To Try
1. Add a few products to the cart; note the small quantity badge appears on the product image
2. Update quantities and see totals change instantly
3. Remove an item: the button shows “Removing...” until complete
4. Checkout and view the mock receipt
5. Open the site on another device/browser — carts are isolated per session

## 🛠️ Troubleshooting
- 403 from `http://localhost:3000/api/*`: ensure `.env` has `REACT_APP_API_URL=http://localhost:5050` and restart the frontend
- Port 5000 conflict on macOS: backend uses 5050 to avoid AirPlay
- Express 5 wildcard: use a final `app.use((req, res) => res.sendFile(...))` fallback instead of `app.get('*', ...)`

## 📌 Hiring Notes
This assignment demonstrates:
- Practical full‑stack wiring (React ↔ Express ↔ SQLite)
- UX details (minimalist styling, image handling, micro‑feedback on actions)
- Session‑scoped state without a full auth system
- Cloud deploy experience (Render/Vercel)

---
Built for soccer lovers — and reviewers who appreciate clean, pragmatic engineering.

